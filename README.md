Vantage Learning — Task 6 React conversion

Original static project: frontend/
React project: react-frontend/

Run the React project:
cd react-frontend
npm install
npm run dev

The conversion follows the supplied Task 6 document: Vite + React, React Router, pages, reusable components, AuthContext, copied CSS, retained legacy JavaScript, and the original project kept as backup.
